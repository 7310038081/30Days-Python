# Tuples are ordered, immutable collections defined using parentheses. They can contain mixed data types.

# 1.count() Returns the number of occurrences of an item.
t1 = (1,2,3,3,3,4,5)
print(t1.count(3))

# 2.index(item) Returns the number of occurrences of an item.
print(t1.index(4))


